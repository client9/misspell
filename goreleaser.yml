# goreleaser.yml

build:
  main: cmd/misspell/main.go
  binary: misspell
  ldflags: -s -w -X main.version={{.Version}}
  goos:
    - darwin
    - linux
  goarch:
    - amd64
  env:
    - CGO_ENABLED=0
  ignore:
    - goos: darwin
      goarch: 386

archive:
  replacements:
    amd64: 64bit
    386: 32bit
    darwin: macOS

snapshot:
  name_template: SNAPSHOT-{{.Commit}}
